name: Heroku CI

on: 
  push:
    branches: [ main ]

  repository_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - run: |
        git fetch --prune --unshallow
